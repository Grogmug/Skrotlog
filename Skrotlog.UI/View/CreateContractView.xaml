<UserControl x:Class="Skrotlog.UI.View.CreateContractView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Skrotlog.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <GroupBox Header="Kontrakt oprettelse" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
                
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Kontrakt - tingel tangel" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="278,0,0,0">
            <StackPanel Orientation="Horizontal">
                <ComboBox ItemsSource="{Binding Materials}" SelectedItem="{Binding SelectedMaterial}" DisplayMemberPath="Type" Width="120" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10" SelectedIndex="0"/>
                <Button Command="{Binding AddCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="50" Height="22" Content="Tilføj" Margin="10,10"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
