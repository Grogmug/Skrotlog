<UserControl x:Class="Skrotlog.UI.View.ContractView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Skrotlog.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <DataGrid ItemsSource="{Binding }" x:Name="ContractOverveiw" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" ColumnWidth="*">
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <TextBlock Height="20" Text="{Binding Comment}" />
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
    </Grid>
</UserControl>
